#include <cstddef>
#include "object_detection/constants.h"

// define object class for detection
const char* coconame[] = {"Baskom", "Celana", "Flare"};
const size_t coconame_size = sizeof(coconame) / sizeof(coconame[0]);
